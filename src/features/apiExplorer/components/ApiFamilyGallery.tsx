/**
 * The family gallery is now the read-only overview (index records as rows
 * plus "Look up by id"). Kept at this path so CategoryPage keeps compiling.
 */
export { default } from "@/features/apiExplorer/workbench/ApiFamilyOverview";
export type { ApiFamilyOverviewProps as ApiFamilyGalleryProps } from "@/features/apiExplorer/workbench/ApiFamilyOverview";

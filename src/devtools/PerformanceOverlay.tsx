/**
 * @deprecated Removed: the diagnostics overlay is gone. Renders nothing so
 * `AppShell`'s import keeps compiling until the shell package deletes it.
 */
const PerformanceOverlay = (): null => null;

export default PerformanceOverlay;
